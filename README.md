# SVM-from-scratch
This project implements a Support Vector Machine (SVM) classifier from scratch with gradient and Sup gradient methods to authenticate banknotes using wavelet transform features. Unlike library implementations
